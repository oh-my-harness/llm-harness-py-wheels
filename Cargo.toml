[package]
name = "llm-harness-py"
version = "1.4.0"
edition = "2024"
license = "MIT"
publish = false

[lib]
name = "senza"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.29", features = ["macros", "py-clone"] }
llm-harness-types   = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER" }
llm-harness-loop    = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER" }
llm-harness-agent   = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER" }
llm-harness-platform = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER" }
llm-harness-subagents = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER" }
llm-harness-workflow = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER" }
llm-harness-sandbox = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER" }
llm-harness-tools = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER" }
llm-harness-inspector = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER", features = ["inspect"] }
llm-harness-mcp = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER" }
session-viewer     = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER" }
llm-harness-strategy = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER" }
llm-harness-knowledge = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER" }
llm-harness-knowledge-local = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER" }
llm-harness-session-recall = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER", features = ["sqlite-bundled"] }
llm-harness-memory = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER" }
llm-harness-audit-jsonl = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER" }
llm-harness-trace-otel = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER" }
parking_lot = "0.12"
chrono = "0.4"
serde_json = "1"
regex = "1"
anyhow = "1"
tokio = { version = "1", features = ["sync", "rt-multi-thread", "time"] }
futures = "0.3"
tokio-util = "0.7"
base64 = "0.22"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["fmt", "env-filter", "registry"] }

[target.'cfg(target_os = "linux")'.dependencies]
# llm-harness-sandbox already in [dependencies]; bwrap feature is on by default.

[target.'cfg(target_os = "macos")'.dependencies]
# llm-harness-sandbox already in [dependencies]; seatbelt feature is on by default.

[dev-dependencies]
pyo3 = { version = "0.29", features = ["macros", "py-clone", "auto-initialize"] }
tempfile = "3"
llm-harness-loop = { git = "https://github.com/oh-my-harness/llm-harness-runtime", rev = "PLACEHOLDER", features = ["test-utils"] }

[features]
# extension-module is intentionally NOT in default: it must be off for
# `cargo test` so the test binaries link against libpython (via the
# `auto-initialize` dev-dependency). maturin enables it automatically
# when building the cdylib wheel, so production builds are unaffected.
default = []
extension-module = ["pyo3/extension-module"]
# test-utils 启用 drive() 测试方法、MockLlmClient Agent 等仅供测试/验证的功能。
# 生产构建（maturin develop --release / build）不应启用此 feature。
test-utils = ["llm-harness-loop/test-utils"]

[build-dependencies]
pyo3-build-config = "0.29"
